'use strict'

const fs = require('fs');

const parsedBitmap = {};
module.exports = exports = (paths, callback) => {
        let file = `${__dirname}/assets`;

        fs.readFile(file, (err, data) => {

            if (err) {
                throw err
            } else {

            }

        })
    }